spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_db
    username: sheryaar
    password: sheryaar
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: pet_db_schema.sql